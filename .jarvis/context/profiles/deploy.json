{
	"version": 1,
	"configured": false,
	"authorized": false,
	"defaultEnvironment": null,
	"apps": [],
	"environments": {},
	"commands": {
		"preflight": "npm run release:preflight",
		"package": "npm run release:local -- --out <reviewed-temporary-directory> --force",
		"deploy": null,
		"smoke": null,
		"rollback": null
	},
	"triggers": [],
	"providerPolicy": {
		"provider": null,
		"forbidBillingActions": true,
		"forbidDestructiveActions": true,
		"forbidDnsMutation": true,
		"requireExplicitOperationalAuthorization": true
	},
	"operationalBoundary": {
		"ownershipEvidence": "Verify per-capability live ownership against docs/migrations/meeting-dispatch-execution-plan.md; this profile does not attest running processes.",
		"runtimeAuthority": "Explicit verified service enrollment or finite authorization; deployment configuration is not authority.",
		"v1ReactivationAllowed": false,
		"requiredOrder": [
			"create an owner-only SQLite-consistent initial backup and verify checksums and modes",
			"restore the initial backup into an isolated destination and verify it",
			"stage exact reviewed V2 artifacts without activation",
			"stop V1 writers",
			"prove zero known writers and take a final quiesced backup",
			"generate and review exact V2 schedules as disabled data",
			"verify and consume the bounded one-shot smoke authorization",
			"start and bind V2 writers",
			"prove exactly one V2 writer",
			"run workflow smoke tests and duplicate-write negative controls",
			"rehearse recovery using a fresh isolated consistent native backup, restore, exact receipts and replay-state reconciliation, and installed V2 readers",
			"verify reversible installation bindings and preserve V1 source and recovery artifacts without reactivating V1",
			"before an actual recovery or replacement, stop affected writers and verify writer exclusion",
			"take a fresh consistent snapshot and reconcile intervening decisions, native checkpoints and uncertain external receipts; never restore a stale or lossy V1 projection",
			"resume only under verified finite authorization or explicit service enrollment without clearing stale leases or replaying consumed authorizations",
			"prove exactly one V2 writer and verify the affected workflows without dummy or repeated publication"
		]
	},
	"evidence": {
		"persist": false,
		"reason": "This shared profile configures no automatic deployment and grants no operational authority. Per-capability authorizations and operational evidence are recorded separately."
	}
}
